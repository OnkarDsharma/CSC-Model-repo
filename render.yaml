services:
  - type: web
    name: csc-risk-model-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn predict_api:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
